{
  "name": "@imhelper/core",
  "version": "1.0.0",
  "description": "统一的消息助手，支持多种协议适配器",
  "type": "module",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "scripts": {
    "build": "rm -f *.tsbuildinfo && tsc --project tsconfig.json && tsc-alias -p tsconfig.json",
    "clean": "rm -rf lib *.tsbuildinfo"
  },
  "keywords": [
    "imhelper",
    "onebot",
    "milky",
    "satori"
  ],
  "author": "凉菜",
  "license": "MIT",
  "files": [
    "/lib/**/*.js",
    "/lib/**/*.d.ts"
  ],
  "dependencies": {
    "eventsource": "^2.0.2",
    "ws": "^8.18.3"
  },
  "devDependencies": {
    "@types/ws": "^8.18.1",
    "tsc-alias": "latest",
    "typescript": "latest"
  }
}

